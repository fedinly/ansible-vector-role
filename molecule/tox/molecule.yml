---
driver:
  name: podman
platforms:
  - name: instance-name
    image: geerlingguy/docker-ubuntu2204-ansible:latest
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
provisioner:
  name: 'ansible'
verifier:
  name: 'ansible'
scenario:
  test_sequence:
    - destroy
    - create
    - converge
    - destroy
